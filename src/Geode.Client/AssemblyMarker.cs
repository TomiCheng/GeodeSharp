namespace Geode.Client;

/// <summary>
/// Placeholder. The real public API surface (IGeodeCache, IRegion&lt;TKey, TValue&gt;,
/// AddGeodeClient extension) is delivered incrementally per the phase plan in
/// /CLAUDE.md.
///
/// Phase 1 starts at <see cref="Protocol"/> with the frame codec.
/// </summary>
internal static class AssemblyMarker
{
}
